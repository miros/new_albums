%h1= "Albums for you, #{@lastfm_login}"
%h2 Recent:
%ul#recent_albums
  =partial(:album, :collection => @recent_albums)
%h2 Upcoming:
%ul#upcoming_albums
  =partial(:album, :collection => @upcoming_albums)
